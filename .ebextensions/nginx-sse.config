files:
  "/etc/nginx/conf.d/sse_proxy.conf":
    mode: "000644"
    owner: root
    group: root
    content: |
      proxy_read_timeout 300;
      proxy_send_timeout 300;
      proxy_buffering off;

commands:
  reload_nginx:
    command: "service nginx reload || true"
